# nono build-image references (copied in for templates)

These build `nono-cli` and won't build in the nogent repo. They're kept only
as patterns; the live nogent image is `docker/Dockerfile` (Chainguard-based).
